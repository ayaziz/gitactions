name: write comments
on:
  issues:
    types: [opened]
jobs:
  comment_issue:
   runs-on: ${{matrix.os}}
   strategy:
     matrix:
      os: ["ubuntu-latest", "windows-latest"]
   steps:
     - name: print secret
       run: echo  ${{secrets.GITHUB_TOKEN}}
        
